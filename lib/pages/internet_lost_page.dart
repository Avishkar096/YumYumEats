

import 'package:flutter/material.dart';

class InternetLostPage extends StatelessWidget {
  const InternetLostPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text('InterNet Lost Page')
        ],
      ),
    );
  }
}
